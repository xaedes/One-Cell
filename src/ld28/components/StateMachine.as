package ld28.components {
	import ash.fsm.EntityStateMachine;
	
	/**
	 * ...
	 * @author xaedes
	 */
	public class StateMachine {
		public var fsm:EntityStateMachine;
		
		public function StateMachine(fsm:EntityStateMachine) {
			this.fsm = fsm;
		}
	
	}

}